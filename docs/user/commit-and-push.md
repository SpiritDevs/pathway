# Commit and push

Commit and Push records a marker in the thread when both steps finish.

If the commit succeeds but pushing fails, the thread shows **Committed** with the local commit ID. Your commit is saved locally; the push error still appears so you can resolve it and push again. If pushing succeeds but creating a pull request fails, the marker shows **Committed and pushed**.
