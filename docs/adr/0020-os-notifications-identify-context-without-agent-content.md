# OS notifications identify context without agent content

An individual OS notification uses the thread title as its title and the event label plus project
name as its body. It does not copy prompt text, commands, file paths, or agent-response excerpts into
the notification. A quiet-hours summary shows thread and event counts without message content. The
platform's notification-preview controls can hide even this limited context when the user wants a
more private lock screen.
