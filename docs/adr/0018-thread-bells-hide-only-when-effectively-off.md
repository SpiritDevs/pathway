# Thread bells hide only when effectively off

The thread bell sits beside the row's settle and status actions. When all four effective event values
are off, the bell appears on row hover or keyboard focus. An all-on bell remains visible. A dotted
bell remains visible for a mixed state. Tooltips and the Alert Menu identify whether the effective
values came from inheritance or thread overrides. This keeps inactive rows quiet without hiding an
active subscription.
