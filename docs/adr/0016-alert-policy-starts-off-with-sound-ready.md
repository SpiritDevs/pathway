# Alert policy starts off with sound ready

New and existing users default to all four global Alert Policy events disabled, while the
Notification Tray remains active. Local sound delivery defaults to enabled with Pathway's default
sound, so enabling a thread bell produces an immediate result. OS notifications default to disabled
and require an explicit settings action plus platform permission. Missing persisted fields decode to
these defaults for existing users.
