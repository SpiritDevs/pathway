# The sound channel owns the only audible cue

Pathway posts OS notifications silently and lets the independently enabled sound channel play the
selected sound once. System default asks the platform to play its alert sound where supported. A
browser that cannot invoke a platform sound uses Pathway's bundled default instead. Sound-only
delivery remains possible, and enabling both delivery channels produces one notification and one
sound rather than two sounds.
