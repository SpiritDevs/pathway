# Notifications settings includes a local Test alert

Notifications Settings includes Test alert to exercise the enabled local delivery channels. It posts
a sample OS notification when that channel is enabled and permitted and plays the selected sound
when sound is enabled. Because the user explicitly requests the test, it bypasses Alert Policy,
quiet hours, and foreground suppression. It creates no Attention Event, Notification Tray record, or
unread state.
