# The first release targets desktop and web

The first thread-alert release includes the bell, Alert Menu, settings, sound, and OS notifications
on desktop and web. Web delivery works while the page is running and does not promise alerts after
the browser closes. Mobile controls, custom sounds, and Alert Policy enforcement are outside this
release. The existing mobile push preference model continues unchanged until mobile adopts the new
policy in a later release.
