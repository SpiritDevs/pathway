# Alert policy never makes old events newly eligible

An Attention Event must pass Alert Policy when it occurs and remain eligible when delayed delivery
runs. Enabling alerts does not make older Notification Tray history eligible for sound, OS
notification, or catch-up summaries. Disabling an event, project, or thread removes matching events
from pending quiet-hours and reconnect summaries. This prevents preference changes from producing
surprise alerts for old work.
