# Thread alerts require Pathway Cloud like the rest of the app

Pathway Cloud sign-in is required before a user can use the app. Thread alerts use the existing cloud
identity, synced Alert Policy, and Notification Tray data. They do not add unsigned policy, event
history, or merge behavior. A local Pathway environment still belongs to a signed-in user. Here,
local describes where the server runs, not an alternate account model.
