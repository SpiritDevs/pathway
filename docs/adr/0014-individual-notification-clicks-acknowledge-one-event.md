# Individual notification clicks acknowledge one event

Selecting an individual OS notification focuses or opens Pathway, navigates to the exact environment
and thread, and acknowledges only that Attention Event. It does not mark unrelated notifications
read. The notification read model therefore combines the existing all-read watermark with per-event
acknowledgements. Selecting a quiet-hours summary opens the Notification Tray, whose existing open
action marks the whole tray read.
