# Thread alerts have command-palette entry points

The command palette includes Toggle alerts for current thread and Open Notifications settings.
Toggle alerts performs the same four-event bulk action as clicking the thread bell. It is available
for user assignment in Keybindings but ships without a default shortcut. The settings command makes
delivery, project, and schedule controls reachable without navigating the settings sidebar.
